const step=(n,title,copy)=>`<div class="row"><span class="step">${n}</span><div><h3>${title}</h3><p>${copy}</p></div></div>`;
const card=(n,title,copy,to)=>`<a class="card card-link" href="#${to}"><span class="num">${n}<span class="arrow">↗</span></span><h3>${title}</h3><p>${copy}</p></a>`;
const heading=(eyebrow,title,intro)=>`<div class="eyebrow">${eyebrow}</div><h1>${title}</h1><p class="intro">${intro}</p>`;
const pages={
home:()=>`<div class="title-row"><div>${heading('Studio / B Block','What will you make today?','Start small. Make one thing work. Then find out what else it can do.')}</div><span class="pill">YEAR 03 · FALL</span></div><div class="feature"><div class="feature-copy"><div class="eyebrow">Opening project · sample brief</div><h2>Make a scene<br>respond.</h2><p>Turn a still image into a small interactive world. Start with one shape, one change, and one response.</p><a class="button" href="#project">Open the project <span aria-hidden="true">→</span></a><p class="draft">Focus: variables · motion · input</p></div><div class="experiment"><div class="experiment-label"><span>EXPERIMENT 001 / REPEATED MOTION</span><button id="pause">Pause</button></div><canvas id="studio-canvas" role="img" aria-label="A grid of circles moving in waves; an original illustrative coding example"></canvas><div class="experiment-label"><span>One rule. Many different positions.</span><span>sin(t + offset)</span></div></div></div><div class="section-head"><h2>Your next three moves</h2><span>A SMALL, WORKING VERSION</span></div><div class="grid">${card('01 / BUILD','Give it a starting point','Choose a shape. Decide where it begins and what changes.','project')}${card('02 / UNDERSTAND','Once, or every frame?','Explore what setup() and draw() each do.','learn')}${card('03 / CAPTURE','Save a turning point','Keep one image and one decision for your project story.','portfolio')}</div><div class="section-head"><h2>Keep these close</h2><span>SUPPORT WHEN YOU NEED IT</span></div><div class="grid">${card('DEBUGGING','It moves too quickly','Trace a changing value before changing more code.','debug')}${card('NEXT HORIZON','From one to many','See how a single entity can become an Arrayed World.','world')}${card('OUTSIDE THE CLASSROOM','Borrow an idea. Credit it.','Study a creative coding example and change one rule.','community')}</div><p class="draft">Preview content: this opening brief illustrates the site experience. Live assignments and meeting details are still to be set.</p>`,
project:()=>`${heading('Projects / Interaction','Make a scene respond.','Create a small scene that changes over time and responds to a deliberate action. Make its behavior easy to explain.')}<span class="tag">SAMPLE BRIEF</span><span class="tag">PROCESSING JAVA</span><span class="tag">HELP + EXPLANATION</span><div class="two"><section><div class="panel"><h2>A small version is enough</h2><p>A moving dot, a growing garden shape, or a simple drawing tool can all begin with the same idea: a value changes, and the picture changes with it.</p>${step('01','Compose a scene','Choose a canvas size and draw an intentional arrangement. Sketch your starting state before coding.')}${step('02','Make one value change','Use a variable for position or size. Predict what happens after ten frames, then check your prediction.')}${step('03','Respond to an action','Use a click or key event for one clear change. Decide whether holding an input should repeat it.')}${step('04','Test a boundary','What happens when the shape reaches an edge? Choose a rule and test it.')}${step('05','Explain and revise','Trace one changing value. Ask a peer to try the scene, then improve one confusing behavior.')}</div><div class="panel"><h2>Choose a direction</h2><div class="tag">ART</div><div class="tag">TOOL</div><div class="tag">SIMULATION</div><p>Change the theme while keeping the first version small. An extension could add a second entity or let the user change the speed.</p><p>Keep this scene for later units if you want to watch it grow. Starting a new project is equally welcome.</p></div></section><section><div class="panel tint"><h3>Before you call it finished</h3><div class="checklist"><label><input type="checkbox">I can explain what runs once and what repeats.</label><label><input type="checkbox">One changing variable affects the image.</label><label><input type="checkbox">My input and boundary rules behave as intended.</label><label><input type="checkbox">I saved one revision and credited help.</label></div><p class="caption">A thinking checklist, not an automatic score. Selections reset when you leave this page.</p></div><div class="panel"><h3>Useful right now</h3><p><a class="text-link" href="#learn">setup(), draw(), and changing state →</a></p><p><a class="text-link" href="#debug">A value changes too often →</a></p><a class="text-link" href="examples/MotionStudy/MotionStudy.pde" download>Download an original motion starter ↓</a></div><div class="note"><strong>Getting help</strong><br>Bring your goal, expected result, actual result, and one thing you tried. Explain and test any code you adopt.</div><p class="draft">Submission will stay in Classroom. A teacher-provided assignment link will go here when this brief is adopted.</p></section></div>`,
learn:()=>`${heading('Learn / State and time','Once. Then again.','setup() establishes the starting conditions. draw() repeats, so a small change can become motion.')}<div class="two"><section><div class="lesson-demo"><canvas id="motion-canvas" role="img" aria-label="A circle moves horizontally and wraps to the left edge"></canvas></div><div class="controls"><label for="speed">Change per frame</label><input id="speed" type="range" min="0" max="6" step="1" value="2"><output id="speed-value" for="speed">2 px</output><button class="button secondary" id="pause">Pause</button><button class="button secondary" id="reset">Reset</button></div><p class="caption">Browser illustration of the rule below, not a Java runner. Live frame rate varies; paused examples stay still until you press Play.</p><pre><code>float x = 40;
float speed = 2;

void setup() {
  size(600, 240);
}

void draw() {
  background(23, 41, 36);
  x = x + speed;
  if (x &gt; width + 15) {
    x = -15;
  }
  fill(212, 243, 108);
  noStroke();
  circle(x, height / 2, 30);
}</code></pre><a class="text-link" href="examples/MotionStudy/MotionStudy.pde" download>Download the Processing sketch ↓</a></section><section><div class="panel tint"><div class="eyebrow">Predict before you run</div><h3>Where is x after ten frames?</h3><p>Start at 40. Add 2 each frame. Assume the circle has not reached the edge.</p><details><summary>Check your reasoning</summary><p>40 + (10 × 2) = 60. Each frame uses the previous value of x.</p></details></div><div class="panel"><h3>Try a change</h3><p>Move <code>float x = 40;</code> inside <code>draw()</code>. What do you expect to happen?</p><details><summary>Reveal a hint</summary><p>A local variable is created again on every call to draw(). Its previous value is not carried into the next call.</p></details><details><summary>Explain the result</summary><p>The local x starts at 40 and becomes 42 every frame. The circle appears stationary. Moving the declaration outside draw() preserves its changing state.</p></details></div><div class="note"><strong>Java connection</strong><br>This is variable scope and lifetime. Processing calls the sketch lifecycle methods; your assignments still follow Java rules.</div><a class="text-link" href="#project">Apply this to your scene →</a></section></div>`,
debug:()=>`${heading('Get unstuck / A repeatable method','What did you expect to happen?','Make the problem small enough to see. Investigate one cause, change one thing, and check again.')}<div class="two"><section><div class="panel"><span class="tag">INPUT</span><h2>My toggle keeps switching.</h2><p>You hold a key, but the scene flickers between two modes.</p><pre><code>void draw() {
  if (keyPressed) {
    running = !running;
  }
}</code></pre><details><summary>1. Find out how often this runs</summary><p>Add <code>println(running);</code> inside the condition. Hold the key briefly. How many values appear? Remove the print statement after investigating.</p></details><details><summary>2. Connect the symptom to the rule</summary><p>draw() repeats while the key is held. Every true test reverses running again. You intended one change, but wrote a change for every frame.</p></details><details><summary>3. Try an event, then test it</summary><p>Move the toggle into <code>void keyReleased() { running = !running; }</code> for one change when a key is released. This intentionally changes the action from press to release. Test a tap, a long hold, and a second tap. Add a key condition if only one key should control it.</p></details><details><summary>4. Explain the repair</summary><p>Which code repeats every frame, and which runs on the release event? If you need a press action instead, investigate a keyDown guard and reset it on release to handle repeated key events.</p></details></div><div class="panel"><h2>Other small investigations</h2><details><summary>My shape will not move</summary><p>Print its position. Is it reset inside draw()? Is the drawing command using the changing variable? <a href="#learn">Review state and time.</a></p></details><details><summary>My condition is never true</summary><p>Write down one value that should satisfy it. Can that value satisfy every part? A value cannot be both greater than 300 and less than 120.</p></details><details><summary>My button looks bigger than its active area</summary><p>Draw the bounds used by the hit test. Check the center, each edge, and a point outside. Use shared position and size variables for drawing and testing.</p></details></div></section><section><div class="panel tint"><h3>Bring a useful question</h3>${step('01','Goal','What should someone see or do?')}${step('02','Expected / actual','What did you predict, and what happened?')}${step('03','Small example','What is the smallest sketch that still shows it?')}${step('04','Attempt','What did you test, and what did it reveal?')}</div><div class="note">A useful help session ends with a change you can explain and test. Credit consequential help in your project story.</div></section></div>`,
portfolio:()=>`${heading('Your portfolio / Show your thinking','A collection of turning points.','Keep your Google Site. Build four project stories that show what you made, how it changed, and what you now understand.')}<div class="two"><section><div class="panel"><div class="eyebrow">Original model story · not student work</div><h2>Motion study: from stuck to moving</h2><p>I wanted one circle to travel across the screen and return smoothly at the left edge.</p><span class="tag">STATE</span><span class="tag">REVISION</span><h3>The turning point</h3><p>My first version declared x inside draw(). The circle stayed at 42 because every frame started from 40 again. I moved the declaration outside draw() so each update could use the previous position.</p><pre><code>// Before: a fresh x on every frame
void draw() {
  float x = 40;
  x = x + 2;
}

// After: x retains its state
float x = 40;
void draw() {
  x = x + 2;
}</code></pre><p class="caption">Focused excerpts: drawing commands are omitted. Use the full sketch below to run the example.</p><h3>How I checked it</h3><p>I traced ten updates from 40 to 60. I then tested a speed of zero and watched the circle stop. Finally, I checked the wrap rule at the right edge.</p><h3>What I would change next</h3><p>The movement is per frame, so slower rendering means slower motion. Next I would investigate elapsed time before adding more circles.</p><h3>Source and assistance</h3><p>This teacher-facing model was authored for the site mockup with AI assistance. It illustrates a reflection structure; it is not a student's submission.</p><a class="text-link" href="#learn">See the mechanism in motion →</a></div></section><section><div class="panel tint"><h3>Your project story</h3><ol><li>A title, intent, and short demonstration.</li><li>One mechanism explained with code or a diagram.</li><li>A before-and-after change and its reason.</li><li>A bug investigation and what it taught you.</li><li>Sources, assistance, and a runnable package.</li></ol><a class="button" href="examples/project-story.md" download>Download the story template ↓</a></div><div class="panel"><h3>Five minutes at the end</h3><p>Save one image or clip, one decision, and one next step. Build your story from these small records.</p><h3>Before sharing</h3><p>Ask a peer: can you tell what it does, explain one mechanism, and follow the run instructions?</p><p>Keep drafts and private notes with the class. Choose a wider audience with your teacher.</p></div><a class="text-link" href="examples/MotionStudy/MotionStudy.pde" download>Example source ↓</a></section></div>`,
community:()=>`${heading('Beyond class / Learn in public, at your pace','Join the conversation.','Programmers learn by reading, adapting, asking, and sharing. Start with a small idea connected to your own work.')}<div class="grid">${card('01 / NOTICE','Find one mechanism','Look at a sketch. Explain one rule that makes it interesting.','learn')}${card('02 / ADAPT','Change something on purpose','Keep the original link. Show exactly what you changed.','project')}${card('03 / SHARE','Make your thinking useful','Write a question, improve instructions, or explain a revision.','portfolio')}</div><div class="section-head"><h2>One small activity</h2><span>READ → REMIX → CREDIT</span></div><div class="two"><div class="panel"><h2>What changes when you change one rule?</h2><p>Find a beginner Processing example that uses shapes or motion. Predict the effect of changing spacing, speed, color, or a boundary rule. Make one change and compare the result.</p>${step('01','Give credit','Keep the source URL and author credit with your code.')}${step('02','Explain the difference','“The original does ___. I changed ___ because ___.”')}${step('03','Invite a response','Ask a classmate one specific question about the result. Use their observation to revise it.')}<div class="note">A thoughtful classroom contribution counts. Public posting and external replies are not required.</div></div><div class="panel"><h3>Places to begin</h3><p><a href="https://happycoding.io/tutorials/processing/">Happy Coding ↗</a><br><small>Processing Java · Start with a shapes or animation tutorial.</small></p><p><a href="https://thecodingtrain.com/challenges/">The Coding Train ↗</a><br><small>Check the language first · Identify one useful technique.</small></p><p><a href="https://discourse.processing.org/">Processing Community Forum ↗</a><br><small>Read first · Notice how a clear question includes a small example.</small></p><p class="caption">Resource entry points checked September 7, 2026. Plan accounts or external sharing with your teacher.</p></div></div>`,
world:()=>`${heading('Projects / Later in the year','Arrayed World','Start with one independent entity. Give a whole population behavior through shared rules.')}<span class="tag">LATER PROJECT PREVIEW</span><div class="two"><div class="panel">${step('01','One object','Display it, update it, and give it a boundary rule.')}${step('02','A population','Construct each element in an object array. Traverse using its length.')}${step('03','Meaningful variation','Give instances different positions, speeds, colors, or sizes.')}${step('04','Interaction','Define what mouse and keyboard input do. Test the boundaries.')}${step('05','A useful revision','Make the world easier to understand or extend. Save the comparison.')}<p class="note">Possible worlds: a moving constellation, a population following simple rules, or an interactive field of marks. The theme is yours.</p></div><div class="panel tint"><h3>Explain before extending</h3><p>What exists after an array is allocated but before its objects are constructed?</p><details><summary>Think it through</summary><p>An object array initially holds null references. You must construct each object before calling its methods.</p></details><h3 style="margin-top:24px">A test worth trying</h3><p>Change the population size. Does every object still update without editing the traversal loops?</p><p class="draft">This preview shows a later project-page structure. The final year-three requirements remain a teacher decision.</p></div></div>`
};
let cleanup=()=>{};
function render(){cleanup();const key=location.hash.slice(1)||'home';const page=pages[key]?key:'home';document.querySelector('#main').innerHTML=pages[page]();document.title=`${{home:'Studio',project:'Make a scene respond',learn:'State and time',debug:'Get unstuck',portfolio:'Your portfolio',community:'Beyond class',world:'Arrayed World',arc:'Course arc',arrays:'Arrays of objects',story:'Build a project story',library:'Reference shelf',projects:'Projects',start:'Start in Processing',coordinates:'Coordinates',input:'Events and frames',packaging:'Make it runnable'}[page]} · Intro Java`;document.querySelectorAll('nav a').forEach(a=>{if(a.hash==='#'+({world:'projects',project:'projects',learn:'library',arrays:'library',start:'library',coordinates:'library',input:'library',story:'portfolio',packaging:'portfolio'}[page]||page))a.setAttribute('aria-current','page');else a.removeAttribute('aria-current')});if(page==='home'||page==='learn')startDemo(page);initPage(page);window.scrollTo(0,0);}
function startDemo(page){const canvas=document.querySelector('canvas'),ctx=canvas.getContext('2d');let paused=matchMedia('(prefers-reduced-motion: reduce)').matches,frame=0,x=40,raf;const pause=document.querySelector('#pause');pause.textContent=paused?'Play':'Pause';pause.onclick=()=>{paused=!paused;pause.textContent=paused?'Play':'Pause'};const speed=document.querySelector('#speed');if(speed)speed.oninput=()=>document.querySelector('#speed-value').textContent=speed.value+' px';const reset=document.querySelector('#reset');if(reset)reset.onclick=()=>{x=40};function paint(){const w=canvas.clientWidth,h=canvas.clientHeight,dpr=devicePixelRatio||1;if(canvas.width!==Math.round(w*dpr)||canvas.height!==Math.round(h*dpr)){canvas.width=Math.round(w*dpr);canvas.height=Math.round(h*dpr)}ctx.setTransform(dpr,0,0,dpr,0,0);ctx.clearRect(0,0,w,h);if(page==='home'){for(let row=0;row<6;row++)for(let col=0;col<10;col++){const t=frame*.018+col*.38+row*.45;ctx.beginPath();ctx.arc((col+1)*w/11,(row+1)*h/7+Math.sin(t)*10,3+(Math.sin(t)+1)*3,0,Math.PI*2);ctx.fillStyle=(col+row)%4===0?'#d4f36c':'#91bea0';ctx.fill()}}else{ctx.scale(w/600,h/240);ctx.strokeStyle='#3d574a';ctx.beginPath();ctx.moveTo(0,120);ctx.lineTo(600,120);ctx.stroke();ctx.beginPath();ctx.arc(x,120,15,0,Math.PI*2);ctx.fillStyle='#d4f36c';ctx.fill();if(!paused){x+=Number(speed.value);if(x>615)x=-15}}if(!paused)frame++;raf=requestAnimationFrame(paint)}paint();cleanup=()=>cancelAnimationFrame(raf)}

